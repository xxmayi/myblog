# 2026-08-18

- 文章详情页左侧目录改为「按栏目分组显示」（CATEGORY_ORDER 固定顺序：日常碎碎念/旅行足迹/读书观影/美食厨房/手作），分组标题用栏目色，高亮当前文章。改动 `[slug].astro` 与 `global.css`。
- 输出文档 `腾讯云COS开通与备案指南.md`（根目录）：面向小白的分步清单——实名认证→开 COS 桶→拿 API 密钥(.env 交付)→ICP 备案(1-2周)→备案后绑域名/CDN。强调备案可并行、密钥安全、COS 可作备案资源。
- 预览服务：`npm run preview` 在 http://localhost:4321 正常；dev 模式因沙箱 safe-delete 钩子仍起不来（旧坑，已记入 MEMORY.md）。
- 部署方案变更：用户改主意，**从腾讯云改为 GitHub Pages（免备案、免费）**。已：① 改 `astro.config.mjs` 的 site 为 GitHub Pages 占位 + 用户页/项目页注释；② 新建 `.github/workflows/deploy.yml`（push main 自动部署）；③ `.gitignore` 补 `.env` 忽略；④ 输出 `GitHub-Pages部署指南.md`（小白分步）。旧的腾讯云文档不再适用。
- 待用户确认：GitHub 用户名 + 仓库名（用户页/项目页），以便把 site 填成真实地址；或用户自行填。
- 用户确认：GitHub 用户名 `xxmayi`、项目页仓库 `myblog` → 网址 `https://xxmayi.github.io/myblog`。已把 `astro.config.mjs` 的 `site` 填为 `https://xxmayi.github.io`、`base: '/myblog/'`。
- 修复项目页 base 坑：所有内部链接（Nav/Footer/PostCard/index/categories/[category]/[slug]/rss/about/RSS）改为 `import.meta.env.BASE_URL + 路径` 前缀；并顺手把栏目页/归档页遗留旧站名「林间小记」改为「我的小世界」、about 页「云服务器」改为「GitHub Pages」。构建验证：40 页生成，产物内 `/myblog/` 前缀全到位、无裸绝对路径残留。本地预览地址变为 http://localhost:4321/myblog/（K9iIcB 进程）。
